# Revenue Dashboard

A React + Vite revenue/MRR dashboard.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## Deploy

Static build, deployable on Vercel (config in `vercel.json`):
`npm run build` → outputs to `dist/`
